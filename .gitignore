# Local Claude Code settings (not needed on GitHub)
.claude/

# OS junk files
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Upload bundles — temporary zips made for uploading frames to WordPress.
# The frames themselves are already tracked, so these are disposable.
hero-seq.zip.zip
*.zip

# Exported chat/account data - private, never publish
Chat's/

# Generated local preview. Built from scroll-sequence.html by
# make-local-preview.js, so tracking it would just be a stale duplicate that
# silently drifts from the real file. Rebuild it with:
#     node hero-sequence/make-local-preview.js
hero-sequence/scroll-sequence.local.html

# مخرَجات graphify · بتنبنى محلياً بأمر واحد وبلا كلفة
# python -m graphify update "D:/Ryan-Work/LUVIT/luvit"
graphify-out/

# صور المنتجات الأصلية · بتنحط بـ D:\Ryan-Work\LUVIT\صور-المنتجات
# برّا الريبو أصلاً · وهاد السطر عشان لو حدا نسخهم جوّا بالغلط
صور-المنتجات/

# 🔴 مفتاح OpenRouter · سرّي · ريّان بيحطه بإيده ولا مرة بينرفع
_أدوات/.openrouter-key
.openrouter-key

# فريمات تجارب القطرة · مشتقّة بالكامل من الـmp4 المتتبَّع جنبها،
# وبتنبني بثواني بـextract_frames.py. تتبّعها = نسخة تانية بتنحرف بصمت.
# (نفس منطق scroll-sequence.local.html فوق.)
hero-sequence/frames-drop-test*/
