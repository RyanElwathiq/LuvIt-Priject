1 · BAND RHYTHM BREAKS AT p4/p5 — the aggregate defect the question predicted, and it cannot be fixed by touching one file. p4 and p5 are BOTH declared band-mist. The declared run is L M L M M L M dark. Every downstream repair collides with a component background, because I grepped what each component actually paints: .luvit-card 205 = --color-paper (#FFFFFF), .luvit-ing__row 3324 = --color-paper, .luvit-acc__item 2887 = --color-paper, .luvit-option 2067ff = --field-bg (#FFFFFF), and .luvit-trust__item 2765 paints NOTHING. band-light is #FFFFFF and band-mist is #FAFCFD. So flipping p5 to light puts white rows on white and then collides with p6; flipping p4 to light collides with p3. The only phase that alternates AND lands every white surface correctly is to re-phase from the top: p1 mist · p2 light · p3 mist · p4 light · p5 mist · p6 light · p7 mist · p8 dark. Cards (p2, p4) land on paper exactly as the home page already ships its product grid (home-preview.html:1513); tiles, ingredient rows and the accordion land on canvas where their borders read; the transparent trust row is band-agnostic either way. Corrected html for p1-p4 carries the flip; p5, p6, p7 keep the bands they were given. Cost: four files, and the three cleanest sections are not touched.

2 · band-light / band-mist DO NOT EXIST IN tokens.css — page-level blocker for all eight. grep returns zero. They are declared only in preview harnesses: home-preview.html:33-35 and products-preview.html:38-39. Every existing home section relies on that, so it is pre-existing house behaviour and not charged to any one section, but /products/ is a NEW page: unless those three rules move into the WPCode header CSS snippet (or each Elementor container gets its background set by hand), all eight bands render transparent on the live site and the whole rhythm above is decorative. Fix this before any of the eight is pasted.

3 · THE SAME UNVERIFIED NUMBER, TWICE — «أربع أسئلة» in p3 and again in p8. Both are plan-verbatim, and both contradict what is live: 04-quiz.html:73 reads «أقل من دقيقة · ٥ أسئلة بس». Nobody can settle it because /quiz does not exist. Reviewed section by section this reads as plan compliance; read together it is the page shipping one wrong number in two places. Removed from both. When /quiz is built, one number goes into three files in the same commit: p3, p8, 04-quiz.html.

4 · THE SAME ONWARD LINK WITH THE SAME LABEL, TWICE — /quiz with the button text «اكتشفي روتينك» in p3 and byte-identical in p8. The plan sanctions both destinations (§3 quiz line, §8 CTA), so the link is not the defect; the identical label is. Five sections apart, the reader hits the same button twice and the page reads as a closed loop rather than a descent. Differentiate p3's ("مش عارفة نوعك؟ ابدأي من هون" or similar) and keep p8's as the terminal call. Noted in p8's corrected header.

5 · «نِسَب … مكتوبة» THREE TIMES — p1 H1 «التشكيلة كاملة، والنِسَب مكتوبة», p4 sub «كل منتج بنِسَبه مكتوبة», p5 sub «نِسَب مكتوبة على العبوة نفسها». The percentages are the page's one true differentiator, and stating the same thing three times in eight sections spends it instead of building to it. p5 owns the claim and must keep it; p1 is the page's promise and should keep it; p4's sub is the one to rewrite. I did not change it, because it is plan-verbatim copy and belongs to Ryan.

6 · «ادفعي لمّا يوصلك» APPEARS TWICE VERBATIM — p1 sub ends «وادفعي لمّا يوصلك», p6 item 1 note is «ادفعي لمّا يوصلك». Same five words, first and sixth section. On top of that «الدفع عند الاستلام» appears in p6, p7 and p8, plus the site-wide header strip that runs above all of them, so the phrase is on screen four or more times in one scroll. It is the page's strongest trust fact and it is being worn out. Vary p1 or p6.

7 · TWO PAIRS OF SECTIONS OPEN THE SAME WAY — p1 sub and p3 sub both open with the imperative «اختاري». p2 sub and p4 sub both open with «كل + singular noun» («كل بكج فيه…», «كل منتج بنِسَبه…»). Invisible per file, obvious when the eight are read in order.

8 · ONE SKU, TWO SPELLINGS — p2 writes «منظّف هيدرا اللطيف» (with shadda), p4 writes «منظف هيدرا اللطيف» (without), for the same product on the same page. Both are placeholders that the brand owner will replace, but whoever replaces them will replace one and miss the other unless it is flagged now. Noted in p4's corrected header.

9 · THE PAGE DISAGREES WITH ITSELF ABOUT PRICES — p2 typed 27.00 / 36.00 / 46.00 د.أ into the HTML and marked them placeholder; p4 refused, left a commented Woo slot, and cited the plan's global rule (line 27: "Every price on the page comes from WooCommerce, never typed into HTML"). Both authors documented their reasoning and reached opposite conclusions. p2's own header comment states the rule it then breaks. Corrected p2 to p4's pattern, which is the one the plan mandates.

10 · GATE DOCUMENTATION IS INCONSISTENT, AND THE MOST IMPORTANT GATE IS MISSING — p3, p6 and p8 carry explicit do-not-publish-yet blocks in their headers. p2 does not, even though §"3 · THE EMPTY-SHELF PROBLEM" says the packages rail "does not render" until 3 confirmed SKUs exist, at which point section 4 moves to position 2 (and would then take band-light in the corrected phase, unchanged). p2's /shop/?product_cat=packages also points at a category nobody has created yet, and p5's /ingredients has no gate at all. Someone assembling from eight files will paste the ungated ones. Gates added to p2 and named for p5.

11 · SIX DEAD PURCHASE BUTTONS — p2 and p4 each ship three `<button class="luvit-btn">أضيفي إلى السلة</button>` that do nothing. Both headers flag them, so no author was careless; but this is the paid-ad destination, and the page currently has six controls that look like the primary conversion action and are inert. They must all be replaced by the Woo add-to-cart form or the widget in the same pass, not one section at a time.

12 · <bdi> POLICY IS INCONSISTENT — p3 alone wraps its Latin eyebrow (`<bdi>Skin type</bdi>`); p2 ("Packages"), p5 ("What's inside"), p6 ("Delivery") and p7 ("FAQ") do not. Rule 7 is not actually violated anywhere, because a standalone eyebrow is not Latin inside an Arabic run, and the only true embedded case (p5's four ingredient names) IS wrapped correctly. So this is inconsistency, not breakage. Pick one and apply it to all five. Related: p1 and p4 have no eyebrow while five sections do, so the head rhythm is uneven too.

13 · TWO tokens.css GAPS THAT ONLY SHOW WHEN ASSEMBLED — (a) `.luvit-ing` is single-column below 720px, so p5 renders four stacked rows on a phone, roughly doubling its height against the plan's 2500-3500px budget; the plan asks for 2×2. (b) `.luvit-trust` is 2-up then 4-up and p6 has three items, giving 2+1 on mobile and 3-of-4 on desktop instead of the specified 3-up. Neither can be fixed inline. Also worth one class: six inline `style="text-align:center…"` attributes across p2, p3 (×2), p4, p5, p7 would all disappear behind a single `.luvit-section__foot` rule. No section has a `<style>` block, an inline `<script>` or an on* handler, so rule 8 is clean page-wide.

14 · CHECKS THAT CAME BACK CLEAN ACROSS ALL EIGHT — zero `href="#"`. Zero em-dashes, comments included. Zero masculine address to the customer. Zero duplicate headings. Zero id collisions: page-head, packages, skin-types, products, ingredients, delivery, products-faq are all unique and p8 has none; the SVG gradient ids (pk1-pk3 in p2, p1-p3 in p4) live inside `data:` URIs, which are separate documents and cannot reach the page DOM. And nobody assembled the eight and measured total height at 375px, so the 2500-3500px budget is still unverified.